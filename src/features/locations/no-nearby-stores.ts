export function getNoPublishedLocationsTitle(): string {
  return 'No pickup shops available';
}

export function getNoPublishedLocationsSupportingCopy(): string {
  return 'There are no pickup shops available right now. You can still browse the menu.';
}
